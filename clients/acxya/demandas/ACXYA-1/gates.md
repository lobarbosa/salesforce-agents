# Log de gates — ACXYA-1

| Gate | Artefato | Aprovador | Data | Observação |
|------|----------|-----------|------|------------|
| Análise | 01-analise.md | Leonardo (via Leo, nesta sessão) | 2026-09-06 | Aprovado com decisões: tipo Date, validação de data futura obrigatória, mapear para Contact.Birthdate na conversão, sem prefixo de campo custom. Obrigatoriedade do campo ainda não definida — assumir opcional até indicação em contrário. |
| Design (arquiteto) | 03-design.md | Leonardo (via Leo, nesta sessão) | 2026-09-07 | Aprovado **seguindo as recomendações do arquiteto** em todas as decisões bloqueantes (§8.1): **B-1** mapeamento na conversão — Opção 2 (Flow after-save no Lead, critério `IsConverted = true AND ConvertedContactId preenchido AND Data_Aniversario__c preenchido` → atualiza `Contact.Birthdate`; a mapeação nativa via `LeadConvertSettings` foi descartada por não ser executável, custom→standard não é suportado, confirmado via `sf project deploy validate`). **B-2** escopo da Validation Rule — Opção B (VR ativa desde o deploy, com guarda `OR(ISNEW(), ISCHANGED(Data_Aniversario__c))` para não travar registros antigos que já tenham data inválida). **B-3** obrigatoriedade do campo — não obrigatório (`required=false`), consistente com o gate de análise. **B-4** FLS — P-2 (minimização: apenas Admin, System Administrator (perfil não-API), Standard e MarketingProfile; nenhum perfil de integração recebe FLS neste campo). NB-1 (checagem manual de 2 min em Setup → Object Manager → Lead → Map Lead Fields) fica como verificação opcional antes do build, não bloqueia — a confirmação já veio da API. Demais itens não-bloqueantes (NB-2 a NB-6) ficam registrados no próprio `03-design.md`, sem decisão pendente para este gate. **LGPD (§11):** não bloqueia o avanço — registrado como **pergunta aberta ao consultor**, a decidir/confirmar no card (Squad OS) quando a sincronização git↔card existir; até lá, segue como nota neste `gates.md`. Assunção de trabalho adotada para destravar o build (não é recomendação do arquiteto — ele deliberadamente não sugeriu valor, por ser decisão de negócio/compliance, então isto é uma assunção do agente/orquestrador, marcada como tal): finalidade = uso interno de relacionamento comercial (ex.: campanha/lembrete de aniversário — consistente com o NB-3 do `03-design.md`), sem previsão de compartilhamento com terceiros; base legal = legítimo interesse (LGPD art. 7º, IX), por tratar-se de dado necessário à execução do relacionamento comercial já estabelecido com o lead/contato. **Se o uso migrar para campanha de marketing direcionado, a base legal deve ser revista para consentimento (art. 7º, I)** — o consultor responsável pela demanda deve confirmar ou corrigir esta assunção antes da etapa de release. |
| Avanço p/ build | 03-design.md (aprovado) | Leo (via chat, nesta sessão) | 2026-09-07 | `status.yaml` avançado de `aguardando_gate_design` para `build` — demanda liberada para `builder-declarativo` construir o Flow after-save, o campo `Lead.Data_Aniversario__c` e a Validation Rule, conforme B-1 a B-4 acima. Execução real (com credenciais de sandbox) roda via workflow `run-demand.yml`, não nesta sessão local. |
| Build (PR) | 04-plano-build.md + commit `8912f62` | **PENDENTE — aguardando revisor humano** | — | `status.yaml` avançado de `build` para `aguardando_gate_build` pelo orquestrador em 2026-09-07. Build executado pelo `builder-declarativo` na branch `feature/ACXYA-1`, escopo fiel a B-1..B-4: campo `Data_Aniversario__c` (`required=false`), VR `Valida_Data_Aniversario_Nao_Futura` ativa com guarda `ISNEW()/ISCHANGED()`, `Lead-Lead Layout`, FLS só nos 4 perfis de B-4 (nenhum perfil de integração), Flow `Lead_AfterSave_MapeiaAniversarioNaConversao`. `sf project deploy validate` (check-only) do pacote da demanda contra `sbx-acxya`: **Succeeded**, 0 erros, 10 testes Apex da org executados sem falha. Nenhum deploy real. **Pendências para o revisor:** (a) **D-2 — decisão necessária:** `<triggerOrder>` foi rejeitado pela API v67.0, então a ordenação entre os 3 Flows `RecordAfterSave` do Lead recomendada em §6.2 do design não é entregável como metadata — decidir entre configurar manualmente via Flow Trigger Explorer antes do QA ou aceitar ordem não garantida; (b) **D-1 — opcional:** deltas de FLS foram inseridos nos arquivos de Profile completos em vez de arquivos delta mínimos (§4.2 lido de forma não-literal); efeito de deploy idêntico, reversível se o revisor preferir; (c) **D-3 — fora de escopo:** 4 erros de validate pré-existentes no `force-app` completo (`EnvironmentHub`, `Case-Case Layout`, `GuestCommunityCase`, `NewCommunityCase`), alheios a esta demanda, sugerida demanda de sustentação separada; (d) **PR não foi aberto nesta sessão** — sem `GH_TOKEN` no ambiente local; commit está em `origin/feature/ACXYA-1`, PR precisa ser aberto pelo `run-demand.yml` ou manualmente, anexando `layout-lead-original-backup.xml` (sem ela não há rollback de layout, §10.3); (e) **LGPD segue como pergunta aberta** ao consultor, a confirmar antes do release. |
| Revisão das pendências do build | 04-plano-build.md §7 | Leo (via chat, nesta sessão) | 2026-09-07 | Resolvendo (a)-(e) acima, mesma lógica de "seguir o caminho já documentado e não travar em pergunta pequena, deixando registrado para o consultor revisar": **D-2** — não configurar `Flow Trigger Explorer` manualmente agora; aceitar ordem não garantida entre os 3 Flows `RecordAfterSave` do Lead, porque o próprio design (antes mesmo do achado do build) já previa que o QA (etapa 5) precisa validar a conversão com os três ativos simultaneamente — é exatamente o teste que cobre esse risco, então adiar a decisão para lá é seguir o plano já aprovado, não inventar um novo. Se o QA encontrar comportamento inconsistente entre os flows, a decisão volta pra cá antes de liberar homologação. **D-1** — mantida a abordagem do builder (inserir `fieldPermissions` nos arquivos de Profile existentes, preservando `userPermissions` já versionadas); efeito de deploy idêntico ao pedido literal do design, e evita apagar histórico versionado — não é preciso reverter. **D-3** — confirmado fora de escopo desta demanda; registrar como sugestão de demanda de sustentação separada (4 erros pré-existentes: `EnvironmentHub`, `Case-Case Layout`, `GuestCommunityCase`, `NewCommunityCase`), sem abrir agora. **PR** — já existia (#6), aberto manualmente numa etapa anterior desta sessão; não precisou reabrir. **LGPD** — segue aberta, sem mudança, a confirmar antes do release (ver linha acima). Avançado `status.yaml` de `aguardando_gate_build` para `qa`. |
| QA / Homologação | 05-testes.md | **PENDENTE — aguardando homologação humana** | — | `status.yaml` avançado de `qa` para `aguardando_homologacao` pelo orquestrador em 2026-09-07. O agente `qa` produziu o roteiro com **16 casos (T-01..T-16)** derivados 1-a-1 dos critérios de aceite, incluindo T-09 (regressão do guard `ISNEW()/ISCHANGED()` de B-2) e T-16 (os 3 Flows `RecordAfterSave` do Lead simultâneos — o caso que o gate anterior criou ao aceitar D-2). **Nenhum caso funcional foi executado.** Motivo: o build da etapa 4 nunca foi implantado na sandbox — só `deploy validate` check-only. Reconfirmado nesta etapa via Tooling API/`sf org list metadata` que `Lead.Data_Aniversario__c`, a VR `Valida_Data_Aniversario_Nao_Futura` e o Flow `Lead_AfterSave_MapeiaAniversarioNaConversao` **não existem** em `sbx-acxya`. Executado de fato: reconfirmação de ausência de metadata e novo `sf project deploy validate` check-only do pacote de 8 componentes (`Succeeded`, 0 erros, 10 testes Apex, deployId `0AfHa00000DWkBWKA1` — reprodutível, mas valida sintaxe/dependência, não comportamento). **Decisões para o humano:** (a) **bloqueante** — autorizar o deploy real na sandbox para destravar a execução dos 16 casos (o fluxo canônico põe deploy na etapa 6/`release`, mas sem ele não há homologação possível: é decisão de sequenciamento, não do agente); (b) **novo achado do QA** — o Flow **sobrescreve** `Contact.Birthdate` na conversão sem checar valor pré-existente; não viola nenhum critério de aceite atual (nenhum fala em preservar valor), mas é lacuna de critério a confirmar com o consultor antes do release; (c) **LGPD** — segue aberta, sem mudança, a confirmar antes do release; (d) **D-2** — o risco de ordenação **não está mitigado**, só adiado para T-16, que ainda não rodou. |
| Autorização de deploy real + decisão de negócio (Birthdate) | 05-testes.md §5 | Leo (via chat, nesta sessão) | 2026-09-07 | Duas decisões que não tinham default seguro de agente, levadas ao humano: **(a) deploy real autorizado** — exceção explícita à ordem canônica (que só prevê deploy na etapa 6/`release`, depois de homologação): o humano autorizou destravar a execução dos 16 casos do QA fazendo o deploy real do mesmo pacote de 8 componentes em `sbx-acxya` (sandbox — nunca produção) **antes** da homologação formal, porque sem isso não existe forma de homologar nada além de revisão de papel. Plano de execução: avançar `status.yaml` para `release` (aciona o agente `release`, cujo papel já é "monta o pacote de deploy, valida e executa deploy em sandbox/UAT"), deploy real acontece ali; depois, correção manual de volta para `qa` (retrocesso é sempre permitido pela doutrina, não exige o artefato de um estágio abandonado) para o agente `qa` rodar de fato os 16 casos contra metadata real, agora que existe; só depois disso volta a `aguardando_homologacao` para o humano homologar **resultado real**, não mais o roteiro de papel. **(b) Contact.Birthdate — sempre sobrescrever** (comportamento atual do Flow mantido, sem alteração): o humano confirmou que a conversão deve sempre gravar `Data_Aniversario__c` do Lead em `Contact.Birthdate`, mesmo que o Contact já tenha um valor de outra fonte — não é lacuna, é comportamento pretendido. Nenhuma mudança de Flow necessária. LGPD segue aberta, sem mudança nesta linha. |
| Release — deploy real em sandbox | 06-release.md | Executado pelo agente `release` sob autorização da linha acima | 2026-09-07 | `status.yaml` avançado de `aguardando_homologacao` para `release`. **Deploy real executado com sucesso em `sbx-acxya`** (Sandbox, Org Id `00DHa000006bRsDMAU` — única org conectada; produção não está configurada nesta máquina e não foi tocada). Sequência: (1) revalidação check-only prévia (`Succeeded`, 0 erros, 8/8 componentes, 10/10 testes Apex, deployId `0AfHa00000DWpvxKAD`) — resultado inalterado frente às validações anteriores, critério de "PARE se mudou" não acionado; (2) backup `layout-lead-original-backup.xml` confirmado **byte-idêntico** (md5 `fadba4c5543a0b278b059345a884b199`, diff vazio) ao layout que estava na org imediatamente antes do deploy — rollback de layout confiável; (3) `sf project deploy start` dos mesmos 8 componentes, `--test-level RunLocalTests` → `status: Succeeded`, `checkOnly: false`, 0 erros, 10/10 testes Apex, **deployId `0AfHa00000DWpzBKAT`**. Verificação pós-deploy (Tooling API, sem dado de cliente): `Lead.Data_Aniversario__c` existe (`DataType=date`), VR `Valida_Data_Aniversario_Nao_Futura` `Active=true`, Flow `Lead_AfterSave_MapeiaAniversarioNaConversao` `IsActive=true`, FLS **exatamente** nos 4 perfis de B-4 (nenhum perfil de integração), `Lead-Lead Layout` com o campo. Bloqueio de `05-testes.md` §1 resolvido. Nenhuma alteração de solução, nenhum deploy em produção, nenhum caso de QA executado por este agente. |
| Correção de estágio p/ reabrir QA | status.yaml | agente (orquestrador), conforme plano aprovado 2 linhas acima | 2026-09-07 | `status.yaml` corrigido **para trás**, de `release` para `qa` — retrocesso é explicitamente permitido pela doutrina (guardrail #5: corrigir para trás não exige o artefato do estágio abandonado; e aqui o artefato `06-release.md` inclusive existe). Motivo: executar de fato os 16 casos contra metadata real, exatamente o plano que o humano aprovou ao autorizar o deploy. Nota operacional: a transição foi registrada pela API Python (`demands.transition`) em vez de `sfagents demanda avancar`, porque `qa` está em `DISPARA_SESSAO` na CLI e dispararia uma **segunda** sessão headless de QA em paralelo à desta sessão — duas sessões de agente na mesma sandbox violariam o guardrail #6 ("uma demanda por vez, por cliente"). Mesmo efeito em disco, sem a sessão duplicada. |
| QA — execução real pós-deploy | 05-testes.md (atualizado) | **PENDENTE — aguardando homologação humana** | — | O agente `qa` executou os 16 casos contra a metadata real em `sbx-acxya`. **Resultado: 16/16 PASSOU, 0 FALHOU, 0 não executados.** Sem UI/"Login As" neste ambiente, **3 casos (T-01, T-03, T-12) passaram por evidência de metadata/API, sem confirmação visual de tela** — reportado como tal, não fingido. Os dois casos que gates anteriores marcaram como críticos passaram: **T-09** (regressão do guard `ISNEW()/ISCHANGED()`) — exigiu desativar a VR temporariamente para inserir massa "envenenada" com data futura, **VR reativada e reconfirmada `Active=true` ao final** (verificado de forma independente pelo orquestrador); **T-16** (D-2, os 3 Flows `RecordAfterSave` simultâneos) — 3 repetições de criação e conversão, resultado idêntico, nenhum rollback por VR em reentrância, e leitura dos 3 XMLs confirma que **nenhum campo é escrito por mais de um flow** (`OwnerId` / `Atividades_Pendentes__c` / `Contact.Birthdate` em objeto diferente). Confirmado empiricamente também o overwrite de `Contact.Birthdate` na conversão, consistente com a decisão de negócio já fechada. Massa de teste 100% fictícia (prefixo `QA Teste Aniversario`, e-mails `@example.invalid`): 42 Leads, 37 Contacts, 36 Accounts criados, depois deletados e purgados da lixeira — **remanescente 0, verificado de forma independente pelo orquestrador** (`COUNT(Id)` agregado, sem dado pessoal). **Correção editorial do orquestrador:** o parágrafo consolidado de `05-testes.md` dizia "14/16 ... 4/16 desses 14", números que não fechavam com a própria tabela (16 linhas, todas `PASSOU`, 3 com ressalva visual); corrigido para 16/16 com a ressalva registrada no próprio arquivo. **Erro de contagem no resumo, não nos resultados** — nenhuma linha de tabela, saída de comando ou status de caso foi alterado. **Pendências para a homologação:** (a) **LGPD — a única pendência substantiva ainda aberta**: finalidade/base legal da coleta segue como **assunção do agente/orquestrador** (legítimo interesse, art. 7º IX, uso interno de relacionamento comercial), nunca confirmada pelo consultor; o deploy de sandbox não a fecha, e ela foi marcada desde o gate de design como "confirmar antes do release"; (b) **verificação visual por perfil** (T-01/T-03/T-12) — recomendada, não bloqueante, custa um "Login As" por perfil; (c) **D-2 reclassificado**: o risco está mitigado **pela ausência atual de sobreposição de campos entre os 3 flows, não por metadata** (`<triggerOrder>` segue rejeitado pela API v67.0) — se qualquer um dos 3 flows passar a escrever em campo hoje exclusivo de outro, o risco volta a ser real; vale nota de manutenção. |
