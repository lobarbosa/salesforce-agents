# Log de gates — ACXYA-1

| Gate | Artefato | Aprovador | Data | Observação |
|------|----------|-----------|------|------------|
| Análise | 01-analise.md | Leonardo (via Leo, nesta sessão) | 2026-09-06 | Aprovado com decisões: tipo Date, validação de data futura obrigatória, mapear para Contact.Birthdate na conversão, sem prefixo de campo custom. Obrigatoriedade do campo ainda não definida — assumir opcional até indicação em contrário. |
| Design (arquiteto) | 03-design.md | Leonardo (via Leo, nesta sessão) | 2026-09-07 | Aprovado **seguindo as recomendações do arquiteto** em todas as decisões bloqueantes (§8.1): **B-1** mapeamento na conversão — Opção 2 (Flow after-save no Lead, critério `IsConverted = true AND ConvertedContactId preenchido AND Data_Aniversario__c preenchido` → atualiza `Contact.Birthdate`; a mapeação nativa via `LeadConvertSettings` foi descartada por não ser executável, custom→standard não é suportado, confirmado via `sf project deploy validate`). **B-2** escopo da Validation Rule — Opção B (VR ativa desde o deploy, com guarda `OR(ISNEW(), ISCHANGED(Data_Aniversario__c))` para não travar registros antigos que já tenham data inválida). **B-3** obrigatoriedade do campo — não obrigatório (`required=false`), consistente com o gate de análise. **B-4** FLS — P-2 (minimização: apenas Admin, System Administrator (perfil não-API), Standard e MarketingProfile; nenhum perfil de integração recebe FLS neste campo). NB-1 (checagem manual de 2 min em Setup → Object Manager → Lead → Map Lead Fields) fica como verificação opcional antes do build, não bloqueia — a confirmação já veio da API. Demais itens não-bloqueantes (NB-2 a NB-6) ficam registrados no próprio `03-design.md`, sem decisão pendente para este gate. **LGPD (§11):** pendente — finalidade e base legal para a coleta de `Data_Aniversario__c` ainda precisam ser registradas aqui por decisão humana antes de liberar o build; o arquiteto não recomenda um valor, por ser decisão de negócio/compliance. |
